pipeline {
  agent any
  environment {
    PATH = "$PATH:/opt/apache-maven-3.8.2/bin"
  }
  
stages{
stage ('Build'){
    steps{
        sh 'mvn clean package'
    }
}
    stage ('scan'){
    steps{
      WithSonarQubeEnv('sq1'){
         sh "mvn sonar:sonar"
      }  
    }
  }
}
}
